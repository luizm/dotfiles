# kubernetes
abbr -a kx 'set -g __kube_ps_enabled 1 ; kubectx'
abbr -a kn 'set -g __kube_ps_enabled 1 ; kubens'

# other
abbr -a fcleaner 'fork-cleaner --token (cat ~/.github/fork-cleaner)'
abbr -a ubuntu 'docker run -t -i --rm ubuntu bash'
